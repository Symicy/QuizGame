# External configuration
spring.config.import=optional:dotenv:.env

# Server Configuration
server.port=8080
spring.application.name=quiz-online

# MySQL Database Configuration
spring.datasource.url=jdbc:mysql://localhost:3306/quizdb
spring.datasource.username=root
spring.datasource.password=symicy
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver

# JPA/Hibernate Configuration pentru MySQL
spring.jpa.database-platform=org.hibernate.dialect.MySQLDialect
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=true

# Open Trivia API configuration (override if you want to target a different endpoint)
# opentdb.base-url=https://opentdb.com

# Optional admin bootstrap (set values via CLI or .env to auto-create an admin user)
# app.admin.username=Quiz Admin
# app.admin.email=
# app.admin.password=

# Lobby configuration
app.lobby.min-players=1
